# הפעלת המערכת — מדריך התקנה (MEXUZ CRM)

מערכת זו היא CRM לניהול לידים, לקוחות ופרויקטים עבור MEXUZ — חברה שבונה מערכות CRM/ERP וניהול
עבור לקוחות. הקוד נבנה כענף (fork) של מערכת דומה, מותאם מחדש לתחום העיסוק והמיתוג של MEXUZ, ורץ
מעל Supabase משלה (Postgres + Auth + Realtime) — **פרויקט Supabase נפרד משל MEXUZ, לא זה של אף
מערכת אחרת**.

עדיין לא קיים פרויקט Supabase למערכת הזו. כדי להריץ אותה בפועל צריך להקים אחד ולבצע כמה צעדים
חד-פעמיים.

## 0. יצירת פרויקט Supabase

1. היכנס ל-[Supabase Dashboard](https://supabase.com/dashboard) וצור פרויקט חדש (New Project).
2. שמור בצד את ה-**Project URL** ואת ה-**anon key** (Project Settings → API) — ייכנסו לקובץ
   `src/lib/supabaseClient.js` (שלב 4).
3. שמור בצד גם את ה-**Project Reference** (חלק מה-URL, למשל `abcdefghijklmnop`) ואת ה-**service
   role key** (סודי — לעולם לא בקוד הפרונט) — נדרשים בשלבי ה-Edge Functions וה-cron בהמשך.

## 1. הרצת ה-migration

1. פתח את ה-Dashboard של הפרויקט → **SQL Editor**.
2. פתח את הקובץ `supabase/migrations/0001_init.sql` מהריפו, העתק את כל התוכן, הדבק והרץ (Run).
   זהו קובץ יחיד שיוצר את כל הסכימה: טבלאות, הרשאות (RLS), צ'אט, משימות ותזכורות, יומן
   (כולל סנכרון Google אופציונלי), התראות Push, לוג פעילות, נעילת התחברויות, ונתוני קטלוג
   ברירת מחדל (סוגי המערכות שMEXUZ מוכרת, רמות שירות, תוספות, פריסה והטמעה, רשימת ספקים/קבלני
   משנה). **לא** נוצרים משתמשים — זו התחלה נקייה.
3. שני בלוקים בקובץ (`pull-google-calendar-changes` ו-`send-task-push`, דרך `cron.schedule`)
   מכילים placeholders בשם `YOUR_PROJECT_REF` ו-`PASTE_YOUR_SERVICE_ROLE_KEY_HERE` — אלה
   אופציונליים (ראה שלבים 2.5 ו-3 למטה). אם לא רוצים סנכרון Google או Push כרגע, אפשר פשוט
   להריץ את הקובץ כמות שהוא — שני ה-cron jobs יקראו לפונקציות שעוד לא נפרסו וייכשלו בשקט, בלי
   להשפיע על שאר המערכת. לחזור לשלב הזה ולעדכן את שני הבלוקים (מחיקה + הרצה מחדש עם
   `cron.schedule`) ברגע שרוצים להפעיל את היכולות האלה.

## 2. פריסת ה-Edge Functions (ליצירה/מחיקה של משתמשים מתוך המסך "הגדרות")

צריך את ה-[Supabase CLI](https://supabase.com/docs/guides/cli) מותקן אצלך:

```bash
supabase login
supabase link --project-ref <ה-Project Reference שלך>
supabase functions deploy admin-create-account
supabase functions deploy admin-delete-account
```

בלי הפריסה הזו, כפתור "צור משתמש" בהגדרות לא יעבוד (הוא קורא לפונקציה השרתית הזו כדי ליצור
משתמש חדש ב-Supabase Auth בלי לפגוע בסשן שלך).

## 2.5 הפעלת התראות Push אמיתיות (אופציונלי, למשימות ותזכורות)

בלי זה, תזכורות/משימות עדיין מתריעות בתוך האתר (בלשונית "משימות ותזכורות") אבל רק כשהאתר פתוח
בדפדפן. השלבים האלה מוסיפים התראה אמיתית שתקפוץ למכשיר גם כשהאתר סגור.

1. ב-**SQL Editor**, מצא את הבלוק `cron.schedule('send-task-push-every-minute', ...)` בתוך
   `0001_init.sql` (או ב-DB עצמו, אם כבר הרצת), והחלף בו את `YOUR_PROJECT_REF` וגם את
   `PASTE_YOUR_SERVICE_ROLE_KEY_HERE` בערכים האמיתיים שלך, ואז הרץ מחדש רק את הבלוק הזה.
2. צור מפתחות VAPID משלך (למשל עם `npx web-push generate-vapid-keys`) ופרוס את הפונקציה
   (מהטרמינל, אחרי `supabase login` ו-`supabase link` שכבר עשית):

```bash
supabase secrets set VAPID_PUBLIC_KEY=<המפתח הציבורי שיצרת>
supabase secrets set VAPID_PRIVATE_KEY=<המפתח הפרטי שיצרת — לא נשמר בקוד>
supabase secrets set VAPID_SUBJECT=mailto:<אימייל ליצירת קשר של MEXUZ>
supabase functions deploy send-task-push
```

3. עדכן גם את `VAPID_PUBLIC_KEY` בקוד הפרונט (`src/lib/push.js`) לאותו מפתח ציבורי.
4. עכשיו כל משתמש יכול ללחוץ "הפעל התראות" בלשונית **הורדה** (למעלה, מעל הוראות ההתקנה) כדי
   לאשר הרשאת התראות ולהירשם.

> הערה: ב-iPhone זה עובד רק אם האתר כבר נוסף למסך הבית (PWA מותקן) — ב-Safari רגיל אין תמיכה
> ב-Push. באנדרואיד/כרום זה עובד גם בטאב רגיל.

## 3. סנכרון יומן Google (אופציונלי)

1. פרוס את `sync-appointment-to-google`, `pull-google-calendar-changes`, `google-oauth-start`
   ו-`google-oauth-callback` (`supabase functions deploy <שם>`).
2. הגדר סודות (`client_id`/`client_secret` מ-Google Cloud Console, ועוד לפי הערות בראש כל
   קובץ ב-`supabase/functions/`) עם `supabase secrets set`.
3. עדכן את הבלוק `cron.schedule('pull-google-calendar-every-10-min', ...)` באותה שיטה כמו
   שלב 2.5 — `YOUR_PROJECT_REF` וה-service role key.

## 4. יצירת המשתמש הראשון (מנהל)

אחרי שה-migration והפונקציות פרוסות, אין עדיין אף משתמש שיכול להתחבר. כדי ליצור את המנהל
הראשון (שממנו כבר אפשר ליצור את כולם מתוך המסך "הגדרות → ניהול משתמשים"):

1. ב-Dashboard → **Authentication → Users → Add user**, צור משתמש עם אימייל כלשהו (למשל
   `admin@mexuz.internal`) וסיסמה. סמן "Auto Confirm User".
2. העתק את ה-`User UID` שנוצר.
3. ב-**SQL Editor** הרץ (עדכן את הערכים):

```sql
insert into public.profiles (id, username, name, role, is_super_admin)
values ('<ה-UID שהעתקת>', 'admin', 'מנהל ראשי', 'מנהל', true);
```

4. עכשיו אפשר להתחבר לאתר עם שם המשתמש `admin` והסיסמה שנקבעה, וליצור את שאר החשבונות דרך
   הגדרות → ניהול משתמשים.

## 5. עדכון פרטי החיבור בקוד

ערוך את `src/lib/supabaseClient.js` והחלף את `YOUR_PROJECT_REF` וה-`anon key` בערכים האמיתיים
מהפרויקט שיצרת בשלב 0.

## 6. הרצת האתר

```bash
npm install
npm run dev      # פיתוח מקומי
npm run build    # בנייה לפריסה (Vercel / Netlify / כל שירות סטטי)
```

## מה הותאם עבור MEXUZ (ולמה)

המערכת המקורית שממנה זו נענפה שירתה חברת ייצור אריזות — הקטלוג, המחשבון, ולוח התפעול דיברו
במונחי מוצר/גימור/משלוח/ייצור. MEXUZ בונה מערכות CRM/ERP/ניהול ללקוחות, אז ההתאמות המרכזיות:

- **קטלוג**: "מוצרים" הם כעת סוגי מערכות (CRM, ERP, ניהול פרויקטים, ניהול מלאי, נוכחות ומשמרות,
  פורטל לקוחות), כל אחד בכמה חבילות (Starter/Business/Enterprise). מחיר לפי כמות = מחיר למשתמש
  לפי מספר מושבים (seats), באותו מנגנון הנחת-כמות שהיה קיים.
- **"גימור"** הפך ל**"רמת שירות"** (סטנדרט / תמיכה מועדפת / SLA ארגוני) — עדיין מכפיל מחיר, רק
  שהמשמעות העסקית השתנתה מגימור פיזי לרמת שירות/תמיכה.
- **"משלוח/הובלה"** הפך ל**"פריסה והטמעה"** (deployment & onboarding), מתומחר לפי מספר משתמשים
  במקום כמות פיזית.
- **שלבי תפעול** (`ops_status`) הפכו משלבי ייצור פיזי (גרפיקה → דגם → ייצור → אספקה) לשלבי מסירת
  פרויקט תוכנה: אפיון → אישור הצעה → מקדמה → פיתוח → תשלום ביניים → בדיקות ומסירה → נמסר ללקוח.
- **ספקים** נשארו כרעיון (ניהול תשלומים לצד ג'), אבל רשימת ברירת המחדל היא קבלני משנה/ספקי טכנולוגיה
  (מפתחים פרילנס, אחסון ענן, סוכנויות שיווק) במקום ספקי חומרי גלם לאריזות.
- **מיתוג**: שם, לוגו, וערכת צבעים חדשים ומובחנים (כחול-אינדיגו/טורקיז), שונים לגמרי מהמקור.

כל שאר התשתית (התחברות, הרשאות RLS, צ'אט, משימות/תזכורות, יומן, התראות, דוחות, ניהול משתמשים)
היא לוגיקת CRM כללית שלא הייתה תלויה בתחום העיסוק, ונשארה כפי שהייתה.

## התקנה כאפליקציה בטלפון

בתפריט הצד → **הורדה למכשיר** יש הדרכה מותאמת לאייפון (הוספה למסך הבית ב-Safari) ולאנדרואיד
(כפתור התקנה אוטומטי דרך Chrome, עם PWA אמיתי — service worker + manifest). אייקון האפליקציה
הוא כרגע גרסה גנרית ("M" על רקע גרדיאנט כחול-טורקיז, `public/icon.svg`) — אפשר להחליף אותו
בלוגו אמיתי של MEXUZ בכל שלב, פשוט להחליף את הקובץ (מומלץ SVG מרובע).
